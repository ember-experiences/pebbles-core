"""Experimental modules — not part of v0.1 public API.

These files exist from earlier exploration work. They are NOT imported by
the package entry points (`__init__.py`, CLI, engine). Do not rely on them.

They are retained here rather than deleted because:
- They may inform later Scout / Family work
- Git history alone would lose the fact that they were staged to merge

Files:
- context_bridge.py
- entity_extract.py
- recent_context.py

Revisit when Pebbles Scout needs context-enriched matching or Pebbles Family
needs entity-level interest profiles.
"""
